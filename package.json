{
  "name": "pharmadesk-monorepo",
  "private": true,
  "description": "PharmaDesk full-stack monorepo",
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:admin": "pnpm --filter admin dev"
  },
  "workspaces": [
    "packages/*"
  ]
}
